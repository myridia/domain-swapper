name: Plugin asset/readme update
on:
  push:
    branches:
    - main
jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with: 
          path: ./test/wordpress/wp-content/plugins/domain-swapper
      - name: Install
        run: |
              sudo apt-get install -y subversion

      - name: Build
        run: |
              ls ./
              

